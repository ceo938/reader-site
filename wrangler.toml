# Cloudflare Workers(정적 자산) 설정 — 읽을거리
# 배포:  npx wrangler deploy        (wrangler 로그인 상태에서)
name = "reader-site"
compatibility_date = "2026-09-01"

[assets]
directory = "./public"
html_handling = "auto-trailing-slash"
